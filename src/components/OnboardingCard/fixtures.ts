import { OnboardingCardProps } from ".";

export const OnboardingCards: OnboardingCardProps = {
  title: "Create a passport",
  image: "/images/onboarding-passport.svg",
  onClick: () => {
    console.log("Btn onClick !");
  },
};
